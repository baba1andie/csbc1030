export const calculateArea = (length, width) => length * width;
export const calculateVolume = (length, width, height) => length * width * height;
export const calculateSquare = (length) => length * length;
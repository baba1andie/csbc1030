module.exports = { 
    calculateArea: function(length, width) {
        return length*width;
    },

    calculateVolume: function(length, width, height) {
        return length*width*height;
    },
    calculateSquare: function(length) {
        return length*length;
    }
};

